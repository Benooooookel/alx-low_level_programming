#include "main.h"
/**
 * *_memset - function that file the memory with a constant byte
 * @s: is the pointer to
 * @b: is the char to be returned
 * @n: is the number of times the charater to be shown
 */
char *_memset(char *s, char b, unsigned int n)
{
  unsinged int n;
 
    
}
