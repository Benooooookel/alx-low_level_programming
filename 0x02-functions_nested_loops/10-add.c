#include <stdio.h>
#include "main.h"
/**
 * add - function that add number
 * @a: the first number to add
 * @b: the second number to add
 * Return: the result calulated
 */
int add(int a, int b)
{
	int total;

	total = a + b;

	return (total);
}
