#include "main.h"
/**
 * add - function that add two integers and returns the result
 * @first:  first number
 * @sec: sec number
 * Return: return the result of the number add
 */
int add(int first, int sec)
{
	return (first + sec);
}
