#include <stdio.h>
#include "main.h"
/**
 * mul - function that multiplies two integer
 * @a: the first parameter
 * @b: the second paremeter
 * Return: the result of the integer
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
