#include <stdio.h>
#include <stdlib.h>
/**
 * function that prints a strings before the other one is executed
 */
list_t print_str(list_t *head, const char *str)
{
	list_t *print, *temp;

	print = malloc(sizeof(list_t));

	if (print == NULL)
		return (NULL);
	while (temp->str != NULL)
	{
		printf("You're beat! and yet, you must allow,\nI bore my house upon my back!\n");
		temp = temp->next;
	}
	temp = temp->next;
	return temp;

}
