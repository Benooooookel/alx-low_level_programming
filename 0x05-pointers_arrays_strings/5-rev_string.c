#include "main.h"
/**
 * rev_string - function that reverses a string
 * @s: the string to be be reversed
 */
void rev_string(char *s)
{
	int index = 0;

	while (*s != '\0')
	{
		s++;
		index++;
	}
	int rev = index - 1; 
}
